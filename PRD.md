# PRD · 鞋服店铺进销存记账软件（第一版）

- 文档版本：V1.0
- 日期：2026-08-30
- 状态：已确认（作为后续开发依据；如需求变更，先改本文档再改代码）
- 配套文档：《鞋服店进销存记账软件-实施方案.html》（含架构图、流程图、界面原型图）

---

## 1. 产品目标和使用场景

### 1.1 产品目标

为单店经营的鞋服店铺提供一套**零服务器、零费用、手机+电脑通用**的进销存记账软件，解决手工记账"账分多处、库存靠脑记、赠送说不清、利润算不清"的问题。

- 所有单据（进货、销售、赠送、退货、盘点）保存时**自动联动**库存、收支流水、欠款三本账，不需要手工二次登记。
- 数据 100% 存在用户自己的设备上，不依赖任何服务器、账号和年费。
- 手机开单（高频、要快），电脑管理（录进货、对账、看报表），同一套数据。

### 1.2 使用场景

| 场景 | 设备 | 说明 |
| --- | --- | --- |
| 顾客到店买衣服/鞋 | 手机 | **扫商品条码**（或搜索）→确认数量→选收款方式→保存出单，10–20 秒；可连续扫多件 |
| 顾客问"这双还有没有 38 码" | 手机 | 扫吊牌条码 → 立即显示该款售价、颜色、尺码、**各色码库存矩阵**，一眼看到 38 码还有几件 |
| **采购新款到货（主流程）** | **手机** | 【新款建档】只填 名称/类别/颜色/号码 + 进价售价 → **自动生成款号、SKU id、条码** → **蓝牙直连热敏机立即打标签**（产品名+颜色+号码+价格+条码）→ 贴到商品上 |
| 销售时顾客挑好鞋 | 手机 | 扫吊牌条码（同款共用一个码）→ 弹出该款售价与**颜色×尺码库存矩阵** → 点对应色码 → 收款 → 完成 |
| 送货/送样品/自用出库 | 手机 | 同一张销售单中把明细标记为"赠送"，按成本记账不计收入 |
| 到货验收 | 手机/电脑 | 录进货单（按颜色×尺码明细），确认入库，未付款自动挂供应商欠款 |
| 顾客赊账 / 给供应商结款 | 手机 | 开单选"欠款"挂客户台账；付款登记冲减应付 |
| 关店前对账 | 手机/电脑 | 看今日概览，一键备份 |
| 月底看经营情况 | 电脑 | 月销售额、毛利、净利参考、畅销滞销排行、库存资金占用 |
| 换手机 / 换电脑 | 两端 | 导出备份文件→新设备导入恢复 |

### 1.3 运行环境约束（硬性）

- 一部手机 + 一台联网电脑，**不购买任何服务器/域名/云主机**。
- 纯网页应用（HTML + CSS + 原生 JS，零依赖零构建），本地文件双击即可运行，**离线可用**。
- 手机通过浏览器"添加到主屏幕"以类原生 App 方式使用。
- **条码扫码与生成**：**填「名称/类别/颜色/号码」自动生成款号、SKU id 与条码**；**同款共用一个条码（条码 = 款号）**，手机扫码显示该款价格、颜色、尺码与库存矩阵，点一下即可销售；标签用手机蓝牙直连热敏机打印（同款各色码的标签条码相同、文字不同）。为满足摄像头与蓝牙权限要求，手机端建议通过 https 托管打开（免费静态托管，数据仍存本地），并保留"拍照识别 + 手写输入"作为离线兜底（详见第 11 节）。

---

## 2. 整体页面框架和导航结构

单一应用，两套导航布局，按屏幕宽度自动切换（断点：768px）。

### 2.1 手机端（<768px）：底部 Tab 导航

```
底部 Tab：首页 · 开单 · 库存 · 报表 · 我的
```

- **首页**：今日概览 + 快捷入口（见第 3 节）。
- **开单**：销售开单页（含扫码加行、赠送标记、收款方式），日常最高频入口。
- **库存**：库存查询（按款/色/码）、扫码查库存、预警、盘点入口。
- **报表**：日/月销售、利润、排行（手机只读简版）。
- **我的**：二级功能入口 + 系统设置，包含：**新款建档**、进货管理、销售记录、赠送记录、记账中心、供应商/客户、商品档案、**打印机与标签设置**、备份与恢复、密码锁、关于。

### 2.2 电脑端（≥768px）：左侧栏导航 + 内容区

```
左侧栏：👟 我的鞋服店
  📊 经营看板（首页）
  🛒 销售记录
  🚚 进货管理
  📋 库存管理
  🎁 赠送记录
  💰 记账中心
  🤝 供应商/客户
  📦 商品档案
  🛡️ 备份与恢复
内容区：顶部统计条 + 功能表格/表单区 + 操作按钮
```

### 2.3 通用规则

- 所有列表页统一：搜索框 + 筛选（日期/类型/往来单位）+ 分页（每页 ≤ 300 条）+ 行操作（查看/编辑/作废）。
- 所有表单：必填项校验、金额保留两位小数、单号自动生成且不可编辑。
- 单据只能"修改（仅未结清/当日）"或"作废留痕"，**不做物理删除**。
- 搜索框统一支持三种输入：文字（款号/名称）、**扫码（摄像头或扫码枪）**、手写条码数字。扫码枪在电脑端按"键盘输入+回车"模拟，光标在搜索框内即可直接使用，无需额外对接。

---

## 3. 首页（经营看板）的内容结构

首页 = 数据聚合 + 行动入口，自上而下：

1. **今日概览卡片**（数据来自销售单/流水，实时计算）
   - 今日销售额（不含赠送、退货已冲减）
   - 今日销量（件数）
   - 今日毛利（售价−进价快照）
   - 库存预警款数（点击→预警列表）
2. **主行动按钮**：「🛒 立即开单」（最大按钮，直达开单页）+ 右侧「📷 扫码查库存」按钮（点击直接开摄像头，扫吊牌即看库存）
3. **快捷入口网格**：**➕ 新款建档** · 🚚 进货 · 📋 库存 · 💰 记账 · 📊 报表
   - 「新款建档」为采购到货主入口：建档即生成条码并可直接蓝牙打标签（见 11.9）。
4. **待处理事项条**（黄底提醒）：
   - 库存预警 N 款
   - 客户欠款超期提醒（超过 15 天未还）
   - 供应商未结欠款汇总
5. **备份提醒条**：当天未备份时显示「⚠ 今日尚未备份，点此一键备份」，点击即导出。
6. 电脑端额外显示：本月销售额 / 本月毛利 / 净利参考 / 客户欠款总额四张月度卡 + 近 7 日销售柱状图 + 畅销款 TOP5。

---

## 4. 每个模块的用途

| 模块 | 用途（一句话） |
| --- | --- |
| 商品档案 | 商品主数据：款号、颜色尺码矩阵、价格，是所有单据的基础 |
| 进货管理 | 录进货单、管理供应商应付欠款、入库记账 |
| 销售管理 | 快速开单（搜索或扫码）、收款、退货红冲，是收入与利润的来源 |
| 赠送管理 | 赠品/样品/自用/破损出库的登记与成本统计（并入销售单明细类型实现） |
| 库存管理 | 款-色-码三级实时库存、扫码查库存、预警、盘点、变动追溯 |
| 记账中心 | 收支流水（自动+手工）、供应商应付款、客户应收款台账 |
| 报表与利润 | 日/月/年销售、三层利润、畅销滞销排行、库存资金占用 |
| 数据安全 | 一键备份导出/导入恢复、打开密码、操作留痕 |

---

## 5. 每个模块第一版最必要的数据和操作

### 5.1 商品档案（product）

**数据字段（第一版必做）**

| 字段 | 说明 |
| --- | --- |
> **建档只需填 4 项：名称、类别、颜色、号码**（再加进价/售价），**款号、SKU id、条码全部自动生成**（规则见 11.3）。

**A. 商品（款）product —— 同款共用，自动归组**

| 字段 | 说明 |
| --- | --- |
| **款号 styleCode** | **系统自动生成**，格式 = 类别字母 + 3 位流水（如 `X001`、`F012`）；全店唯一，允许手动修改（改后同步该款全部 SKU 的 id 与条码） |
| 名称 | 必填（用户填） |
| 类别 | 必填（用户选）：鞋 X / 服装 F / 裤 K / 配饰 P / 包袋 B / 其他 O（前缀可自定义） |
| 品牌 | 选填 |
| 参考进价 / 默认售价 | 数值，两位小数（各 SKU 可单独覆盖） |
| 状态 | 在售 / 停售 |

**B. 色码（SKU）sku —— 每个"颜色+号码"一条，库存与条码都挂在这一层**

| 字段 | 说明 |
| --- | --- |
| **SKU id** | **系统自动生成**，格式 = 款号 + 2 位色序 + 尺码码（如 `X0010138` = X001 款 / 色 01 / 码 38）；**全局唯一**，作为库存与单据明细的**内部主键**（不进条码） |
| 款号 | 归属的款 |
| 颜色 | 用户填（白 / 黑 / 灰…），同款内按录入顺序自动编号 01、02… |
| 尺码 | 用户填（38 / M / XL…），编码时清洗为安全字符（`38.5`→`385`、`/`→`-`） |
| **条码** | **自动生成 = CODE128(款号)，同款所有色码共用一条码**（如全部为 `X001`）；可改绑为供应商吊牌码（扫码录入） |
| 条码来源 | 系统生成 / 供应商吊牌（记录生成时间） |
| 库存数量 / 预警阈值 | 见 5.5；默认阈值 3 |
| 状态 | 在售 / 停售（跟随款，可单独停售） |

**操作（第一版必做）**：新增、编辑、停售（不删数据）、按款号/名称/SKU id/条码搜索、列表分页、CSV 批量导入存量商品、**扫码建档**（扫吊牌条码自动填入，见 11.5）、**生成条码 + 打印标签**（见 11.4）、列表支持"有无条码""已打印/未打印"筛选。
- **手机端建档（主流程，见 11.9）**：填 **名称 / 类别 / 颜色 / 号码**（可一次多选多个颜色和尺码，批量生成同款多个 SKU）+ 进价 / 售价 → 点【保存并打印】→ 系统即时生成款号、SKU id、条码 → 蓝牙打出标签。

### 5.2 进货管理（purchase）

**数据字段**

| 字段 | 说明 |
| --- | --- |
| 单号 / 日期 | 单号自动（如 P20260830-001） |
| 供应商 | 必选（可即时新建） |
| 明细[] | 款 + 颜色 + 尺码 + 数量 + 进货单价 + 小计 |
| 总金额 / 已付金额 | 自动合计 / 手工填 |
| 欠款金额 | 总金额−已付，自动计算 |
| 备注 | 选填 |

**操作**：新建进货单（色码矩阵批量填数、**扫码逐件入库**）、修改（仅未结清欠款的单）、作废留痕、按日期/供应商筛选查询；确认保存时自动：库存+N、生成进货支出流水（按已付额）、供应商应付+N。

### 5.3 销售管理（sale）

**数据字段**

| 字段 | 说明 |
| --- | --- |
| 单号 / 日期 | 单号自动（如 S20260830-001） |
| 客户 | 选填（挂账欠款时必填） |
| 明细[] | 款 + 颜色 + 尺码 + 数量 + 售价 + **进价快照** + 类型（销售/赠送）+ **条码（当时扫描的条码留底）** |
| 折扣/抹零 | 整单级 |
| 收款方式 | 微信 / 现金 / 支付宝 / 欠款（可组合） |
| 已收金额 / 欠款金额 | 欠款自动 = 应收−已收 |
| 赠送原因 | 明细为赠送时必选：赠品/样品/自用/破损 |

**操作**：快速开单（搜索选品 / **扫码连续加行**→色码选数→收款→保存，目标 20 秒内）、单行改价、整单折扣、销售退货（原单红冲生成退货单：库存+N、收入冲减；**支持扫原商品条码退货**）、按日期/类型查询；保存时自动：库存−N、收入流水/赠送成本流水、客户应收更新。

### 5.4 赠送管理

**数据**：依附于销售单明细（类型=赠送 + 赠送原因），不单独建表；另存赠送记录汇总视图（日期、商品、数量、成本、原因）。

**操作**：开单时一键把明细切换为"赠送"；赠送记录列表按月份/原因筛选；月末赠送成本合计（报表用）。

### 5.5 库存管理（inventory）

**数据**

| 字段 | 说明 |
| --- | --- |
| 库存键 | 款号+颜色+尺码（唯一） |
| **条码** | 款级索引（条码 → 款号 → 该款全部色码库存），同款共用一条；见第 11 节 |
| 当前数量 | 由单据流水实时累计，不直接手改 |
| 预警阈值 | 按款设置（默认 3 件） |
| 变动流水[] | 日期、单据号、方向（+/−）、数量、余额（每次变动留痕） |

**操作**：按款/色/码/预警状态查询；**扫码查库存**（扫条码 → 弹出该款商品卡：售价、颜色、尺码、**颜色×尺码库存矩阵**，点格子即可加入销售单或去盘点）；预警列表（缺码、低于阈值）；盘点（按色码录入实盘数→差异生成"盘点调整单"留痕，**支持扫码盘点**）；点击某款查看该款全部出入明细。

### 5.6 记账中心（ledger / partner）

**数据**

- 流水：日期、类型（销售收入/进货支出/赠送成本/房租/水电/人工/物流/其他）、金额、关联单据号、备注；单据自动生成的流水不可手改。
- 往来单位（供应商/客户同表）：名称、电话、类型、应收/应付余额、欠款明细、还款记录。

**操作**：手工记一笔收支（费用类）；流水查询（日期/类型）；供应商应付清单与付款登记；客户应收清单与收款登记；还款后余额自动冲减。

### 5.7 报表与利润

**数据/口径（第一版固定）**

- 销售收入 = 销售单实收 + 挂账欠款 − 退货
- 销售成本 = Σ 售出数量 × 进价快照
- 毛利 = 销售收入 − 销售成本；毛利率 = 毛利 ÷ 收入
- 赠送成本 = Σ 赠品数量 × 进价快照（单列，不计入销售成本）
- 净利参考 = 毛利 − 赠送成本 − 费用支出（房租/水电/人工/物流/其他）
- 库存资金占用 = 当前库存 × 最新进价

**操作**：日/月/年报表查询；畅销/滞销 TOP5（按毛利/按销量）；导出 CSV（Excel 可开）。

### 5.8 数据安全

**数据**：操作日志（时间、动作、单据号）；备份文件结构见第 7 节。

**操作**：一键导出备份、导入恢复（覆盖前二次确认）、设置/修改打开密码、查看操作日志。

---

## 6. 首页和每个模块之间的关系

首页不存任何数据，全部是**实时聚合 + 入口分发**：

```
                    ┌─ 今日销售额/销量/毛利  ← 销售管理（当日销售单）
                    ├─ 库存预警数           ← 库存管理（阈值判断）
首页（经营看板） ═══┼─ 待处理：欠款提醒      ← 记账中心（客户应收/供应商应付）
                    ├─ 备份提醒条           ← 数据安全（今日是否已导出）
                    ├─ 本月卡片/趋势图/TOP5  ← 报表与利润
                    └─ 按钮/快捷入口 ───────→ 开单 / 进货 / 库存 / 记账 / 报表

数据链路（所有模块共用同一份本地数据）：
  商品（款）──归组──→ 色码 SKU（各自 id + 条码 + 库存）
      └──被引用──→ 进货单 / 销售单 ──保存时──→ 库存增减 + 流水记账 + 欠款更新
                                        └──────→ 报表汇总 + 首页聚合
  扫码 ──条码──→ SKU id ──→ 该色码库存 / 售价 ──→ 直接销售
```

规则：**单据是唯一事实来源**。首页与报表的每个数字都必须能通过"点击下钻"追溯到对应单据；任何模块都不允许出现与单据流水矛盾的静态数字。

---

## 7. 本地数据文件、备份和恢复要求

### 7.1 本地存储

- 主体数据存 **IndexedDB**（库名建议 `shoeErp`），设置项（密码哈希、界面偏好）存 localStorage。
- 统一数据版本号 `schemaVersion`，为未来升级迁移预留。

### 7.2 备份文件要求

- 一键导出为**单个 JSON 文件**，命名：`鞋服店备份_YYYYMMDD_HHmm.json`。
- 内容包含：全部表数据（商品、进货单、销售单、库存流水、往来单位、流水、操作日志）+ `schemaVersion` + 导出时间 + 单据计数摘要。
- 导出渠道：手机→系统分享/保存到文件；电脑→下载到本地。

### 7.3 恢复要求

- 导入时校验：文件结构合法、版本兼容（高版本文件导入低版本程序要明确提示）。
- 恢复为**整体覆盖**，执行前展示"将覆盖当前 N 条数据"的二次确认。
- 恢复完成后自动核对单据计数摘要，并在操作日志留痕。

### 7.4 备份纪律（产品内建提醒）

- 当天未导出 → 首页显示备份提醒条。
- 每周建议导出一份发送到微信传输助手/网盘（异地备份）。
- 清理浏览器缓存、卸载浏览器、更换设备前，**必须先导出**（文档与界面都要提示）。

---

## 8. 第一版必须完成的功能

对应实施方案阶段 1–3，第一版交付即要求以下全部可用：

1. 商品档案：增改停售、搜索、CSV 批量导入；**填 名称/类别/颜色/号码 自动生成 款号 + SKU id + 条码**（见 11.3）、唯一性校验与重复拦截。
2. 进货管理：进货单录入/修改/作废、色码批量填数、供应商欠款自动挂账。
3. 库存管理：款-色-码三级实时库存、变动流水、低库存预警、盘点调整、**扫码查库存**（扫款级条码 → 库存矩阵）。
4. 销售管理：手机快速开单（20 秒目标）、**扫码加行（扫条码 → 弹矩阵 → 点色码）**、单行改价、整单折扣、组合收款、退货红冲（含扫码退货）。
5. 赠送管理：明细级赠送标记 + 原因、赠送记录视图、赠送成本统计。
6. 记账中心：单据自动流水 + 手工费用记账、供应商付款登记、客户收款登记、余额自动冲减。
7. 报表：日/月销售、毛利/净利参考、赠送成本、畅销滞销 TOP5、库存资金占用、CSV 导出。
8. 数据安全：一键备份导出、导入恢复（二次确认）、打开密码、操作日志。
9. 响应式：手机（底部 Tab）与电脑（左侧栏）双布局，同一数据。
10. 离线可用：本地双击打开即可完成全部核心操作（含"拍照识别条码"兜底，见 11.4）。
11. **条码能力（新增，详见第 11 节）**：
    - 款级条码（同款一个条码），扫码即显示价格、颜色、尺码、库存矩阵；
    - 手机摄像头扫码查库存 + 扫码销售（连续模式）+ 扫码建档 + 扫码入库/盘点；
    - **系统生成条码**（无现成条码的商品一键生成，CODE128 默认 / EAN-13 店内码可选）；
    - **条码标签打印（手机蓝牙直连热敏机）**：**手机上建档即可直接打印**；标签含 产品名/颜色/号码/价格/条码，**每个色码一张、同款条码相同（= 款号）、文字不同**；40×30mm 默认模板、203dpi 位图、静区≥2.5mm、条码高 10mm；蓝牙指令适配（TSPL / ESC/POS 可切换）+ 测试页；电脑端备用打印通道与导出 PNG（详见 11.4）；
    - 预留"一码一色码"开关（条码内容切为 SKU id），默认关闭；
    - **采购到货主流程打通**：手机建档 → 自动生成条码 → 蓝牙打标签 → 贴标 → 扫码销售，端到端 ≤90 秒出标签、≤3 秒出库存（详见 11.9）；
    - 条码唯一性校验（四个入口统一）、生成与打印记录、电脑端扫码枪（键盘模拟）与手写条码输入。

## 9. 第一版暂时不做的功能

明确排除，避免第一版失控（均已在架构上预留扩展位）：

1. 云同步 / 多设备实时一致（先用备份文件互传，阶段 4 再评估）。
2. **iOS（iPhone）上的蓝牙直连打印**——Safari 内核不支持 Web Bluetooth，技术限制无法绕过；iPhone 走兜底路径（分享给厂商 App 打印 / 待打印队列在电脑端打印），见 11.4.3。
3. TSPL / ESC/POS 之外的**品牌私有打印指令**（如部分便携机的私有协议）——第一版只做这两套通用指令，若机型不兼容，用"导出 PNG / PDF + 厂商 App 打印"兜底。
4. 打印机驱动配置、纸张自动识别、打印浓度调节等硬件底层适配（由打印机厂商驱动完成，软件只负责输出位图与标准打印页）。
   - 说明 1：**条码标签打印本身是第一版必做**（用户已有可蓝牙直连的热敏标签机，见 11.4）。
   - 说明 2：电脑端 USB 扫码枪**无需开发**——它本质是键盘模拟输入，光标在搜索框内直接可用，因此"不做扫码枪对接"仅指不做独立硬件配置界面。
4. 多员工账号、权限分级、操作审批流。
5. 会员体系、积分、储值、优惠券。
6. 多店铺/多仓库管理。
7. 商品图片上传与图片管理。
8. 供应商对账单自动生成/打印排版、短信微信催款提醒。
9. 采购建议、智能补货等算法功能。

---

## 10. 可以实验检查的验收标准

以下标准全部可实测验证（含自动化测试可覆盖的用例，验收时逐条打勾）：

### 10.0 编码自动生成（新增，最高优先级）

- [ ] 建档只填"名称=小白鞋 / 类别=鞋 / 颜色=白 / 号码=38" → 自动生成 **款号 `X001`**、**SKU id `X0010138`**、**条码 `CODE128(X001)`**，页面保存前有实时预览。
- [ ] 再建"同款 白/39" → 款号**仍为 `X001`**（不新开款），id 为 `X0010139`，**条码仍是 `CODE128(X001)`（同款共用）**。
- [ ] 再建"同款 黑/38" → 颜色序号自动编为 `02`，id = `X0010238`，条码仍为 `X001`。
- [ ] 新建不同款（名称不同）→ 款号顺延为 `X002`，流水按类别各自计数（服装 = `F001`）。
- [ ] 尺码含小数或斜杠（`38.5`、`39/40`）→ 清洗为 `385`、`39-40`，不产生非法字符。
- [ ] 同一款下重复建"白/38" → 提示"该色码已存在（id X0010138）"，不重复生成。
- [ ] 款号手动改为 `X100` → 该款全部 SKU 的 id 与条码同步更新，并提示"需重新打印标签"。
- [ ] 一次勾选 2 色 × 3 码 → 一次生成 6 个 SKU（款号相同，6 个不同 id，但**共用同一条码 `X001`**）。
- [ ] 打开设置里的"一码一色码"开关 → 条码内容切换为 SKU id，扫码直达该色码（预留能力，默认关闭）。

### 10.0B 采购到货主流程（端到端）

- [ ] 手机上新建"小白鞋"，填 2 色 × 3 码、进价 50、售价 129 → 点【保存并打印】→ 自动生成 6 个 SKU 及条码。
- [ ] 保存后进入标签打印页，6 个色码各一行，默认张数正确，可逐行修改。
- [ ] 点【打印】→ 蓝牙标签机逐张打出，标签上**产品名、颜色、号码、价格、条码**五项齐全清晰；**6 张标签的条码完全相同，文字分别为各自的色码**。
- [ ] **从打开建档页到打出第一张标签 ≤ 90 秒**（含填表时间）。
- [ ] 贴"白/38"标签的商品 → 扫该标签条码 → **弹出 X001 商品卡**（名称、售价 129、颜色×尺码库存矩阵）。
- [ ] 点矩阵中"白/38"格子 → 该色码加入销售单 → 收款保存 → **白/38 库存 −1**，同款其他色码库存不变，收入流水生成。
- [ ] **从扫码到加入销售单 ≤ 3 秒**。

### 10.1 核心业务闭环（单据联动）

- [ ] 新建商品（白/36–40）→ 录进货单 3 件 → 库存显示 3；再开销售单 2 件 → 库存变为 1；流水出现对应收入记录。
- [ ] 同一销售单中 1 行销售 + 1 行赠送：应收金额只含销售行；保存后库存各 −1；赠送行生成"赠送成本"流水（金额=进价），**不生成收入流水**；赠送记录视图可查到该行。
- [ ] 销售退货：对已保存单据执行退货 → 库存 +N、当期销售收入按退货金额冲减、生成退货红冲流水。
- [ ] 进货欠款：进货 1000 元、已付 600 → 该供应商应付余额 400；登记付款 400 → 余额归零，生成付款流水。
- [ ] 客户挂账：开单 300 元选"欠款" → 客户应收 +300；收款登记 300 → 余额归零。
- [ ] 欠款单据未结清前可修改，作废后所有联动（库存/流水/欠款）同步回滚，且留操作日志。

### 10.2 利润口径

- [ ] 构造已知数据（进价 50 售价 129 卖 10 件、赠 1 件、费用 500）：报表显示销售收入 1290、销售成本 500、毛利 790、赠送成本 50、净利参考 240，全部与手算一致。
- [ ] 改动商品档案当前进价后，**历史报表数字不变**（进价快照生效）。

### 10.3 库存与预警

- [ ] 阈值设 3，库存降到 2 → 出现在预警列表并计入首页预警数。
- [ ] 盘点录入实盘数与账面不符 → 生成盘点调整单，库存更新且差异留痕。

### 10.4 备份与恢复

- [ ] 导出 JSON → 清空浏览器数据 → 导入恢复 → 商品/单据/库存/欠款/流水与导出前完全一致（按单据计数摘要与抽查明细核对）。
- [ ] 导入损坏文件或非备份 JSON → 明确报错，不破坏现有数据。
- [ ] 恢复前出现覆盖确认提示。

### 10.5 双端与性能

- [ ] 375px 宽手机模拟器：底部 Tab 正常，20 秒内完成一次开单（含搜索、选色码、收款、保存）。
- [ ] 1280px 电脑：左侧栏 + 看板正常，可完成进货单录入与报表导出。
- [ ] 预置 5000 条单据：列表分页加载 <1 秒，开单搜索无肉眼可见卡顿。
- [ ] 断网状态（本地文件打开）：全部第一版功能可用。
- [ ] 设置打开密码后：重新打开需输密码，错误密码无法进入。

### 10.6 条码与扫码（新增）

- [ ] 录入两个相同条码 → 第二条被拦截并提示"条码已被 款号 X001 占用"（款号=条码，全局唯一）。
- [ ] **款级扫码展示**：扫 X001 的条码 → 1 秒内弹出商品卡，正确显示商品名、款号、**售价**、**全部颜色**、**全部尺码**、**库存矩阵（行=颜色、列=尺码，每格数量）**；各格数量与库存页逐项一致。
- [ ] 库存为 0 的色码在矩阵中灰显且不可点击；整款库存为 0 时提示"此款已无库存"。
- [ ] 点矩阵中某一格 → 该色码以 1 件加入销售单，数量可改；**仅该色码库存减少**。
- [ ] 开单页连续扫码：连扫 3 个不同款的条码 → 依次弹出各自商品卡，选完各加 1 行；重复扫同一条码 → 已加入的行数量累加；有提示音/高亮反馈。
- [ ] 扫到未建档条码 → 提示"未找到此条码"并提供【立即建档】，跳转新建商品且条码已自动填入。
- [ ] 手写输入条码数字（不调用摄像头）→ 与扫码结果一致。
- [ ] 电脑端用扫码枪（或键盘输入条码+回车）→ 与手机扫码表现一致。
- [ ] 断网 + 本地文件打开：拍照识别条码可用，识别结果与实时扫码一致（同一条码）。
- [ ] 条码识别准确率：连续扫 20 个不同商品条码，成功识别 ≥19 次；识别失败时给出"重试/手输条码"入口。
- [ ] 扫描一张商品条码照片（非实时摄像头）→ 能正确解出条码（验证兜底方案）。

### 10.7 条码生成与打印（新增）

- [ ] 无条码商品点【生成条码】→ 立即生成唯一条码（默认 CODE128，内容为款号），写入档案并标记"来源=系统生成 + 生成时间"。
- [ ] 对已有条码的商品点【生成条码】→ 不覆盖，需点【重新生成】才覆盖，且覆盖留操作日志。
- [ ] 批量生成：勾选 10 个无条码商品 → 一次性生成 10 个互不重复的条码；若生成规则撞号则自动顺延。
- [ ] 选择 EAN-13 店内码 → 生成 13 位数字、以 20–29 前缀开头、校验位正确，用通用扫码枪可扫出。
- [ ] 打印预览：40×30mm 模板 → 标签含**产品名 / 颜色+号码 / 价格 / 条码图 / 条码下方数字（款号）**；内容不溢出、不被裁切，条码左右静区 ≥2.5mm。
- [ ] **同款标签条码一致**：同一款（2 色 × 3 码）打印的 6 张标签，**条码内容完全相同（都是 X001）**，文字分别显示各自的颜色与尺码，不串号。
- [ ] 条码模块宽：4 字符款号在 40×30mm 标签上模块宽 ≥0.40mm（实测 ≥3px @203dpi）。
- [ ] 手输条码下方数字（款号 X001）→ 与扫码结果一致（破损兜底）。
- [ ] **蓝牙连接**：手机点【连接打印机】→ 能搜到并连上标签机；下次进入自动重连提示；打印机断电时给出"重连 / 改用电脑打印"提示。
- [ ] **指令适配**：测试页用 TSPL 打出乱码时，切换到 ESC/POS 后能正常出标签（反之亦然），选择结果本地保存。
- [ ] **打印测试标签（TEST001）→ 用手机摄像头和扫码枪都能扫出**（首次使用与每次换纸后必做）。
- [ ] **实际打印验证**：打印 5 张不同商品的标签 → 逐张用手机扫 → 均能正确识别并弹出对应款的商品卡（硬验收，不通过则调整模块宽度/静区/浓度）。
- [ ] **连续打印**：一次打 20 张（含多个色码）→ 不错位、不串行、不缺张，中途不卡死。
- [ ] 按"当前库存件数 / 进货数量"填充打印数量 → 进 3 双白/38 就默认 3 张，可逐格修改。
- [ ] 打印后放置 7 天（普通室内环境）再扫 → 仍可识别（验证热敏纸保存性，若失败则建议换三防纸）。
- [ ] 打印记录：打印后列表显示"已打印 + 时间与张数"，支持"未打印"筛选。
- [ ] 电脑端备用通道：打印预览纸张严格为 40×30mm、100% 缩放，不被撑到 A4；导出 PNG 按 203dpi 为 320×236px。
- [ ] **iPhone 场景**：检测到不支持蓝牙时自动隐藏蓝牙按钮，显示"分享给打印机 App / 电脑端代打"入口，不出现点了没反应的情况。

---

## 11. 条码与扫码功能设计（2026-08-30 增补）

### 11.1 条码绑定层级：款级（同款一个条码，条码 = 款号）

> **最终规则（2026-08-31 定稿）**：**同一个款只用一个条码**，不需要每个单件都唯一。条码内容 = 自动生成的**款号**（如 `X001`）。
> 每个"颜色+号码"仍有一个 **SKU id**（如 `X0010138`）作为**系统内部主键**（库存、单据明细引用、盘点追溯用），但**不印进条码**。

- **一条条码对应一款**：`条码 → 款号 → 该款全部色码库存`，扫一次即定位到该款。
- 同款下所有"颜色+尺码"打印出的标签，**条码内容完全相同（都是款号），只有文字不同**（白/38、白/39、黑/38…）。
- 唯一性校验：款号（=条码内容）全局唯一；SKU id 全局唯一。四个入口统一校验（新建 / 编辑 / CSV 导入 / 扫码建档）。
- **可选扩展（暂不启用）**：设置里预留"条码内容 = SKU id（一码一色码）"的开关，若日后想让每个色码有独立条码，打开即可，切换后可批量重打标签。

### 11.2 扫码后展示内容（款级商品卡 + 库存矩阵）

扫到条码后，立即弹出**这一款**的卡片，一屏看完再决定卖哪个色码：

| 区块 | 内容 |
| --- | --- |
| 头部 | 商品名、款号、品牌、类别、**售价**（同款同价；不同色码不同价时显示"¥129 起"并在矩阵各格显示价） |
| 库存矩阵 | **行=颜色，列=尺码**，每格显示该色码库存数；库存 0 灰显不可点；低于预警阈值标黄 |
| 汇总 | 该款总库存件数、在售色码数、最近入库日期、最近销售日期、累计销量 |
| 操作 | **点矩阵格子 → 该色码以 1 件加入销售单**（数量可改）；另有【去盘点】【查看出入明细】【打印标签】 |

### 11.3 编码规则：id / 款号 / 条码自动生成

**用户只需填 4 项**（名称、类别、颜色、号码）+ 进价售价，其余全部自动生成：

```
款号 styleCode = 类别字母 + 3 位流水        例：X001（鞋第 1 款）、F012（服装第 12 款）
SKU id        = 款号 + 2 位色序 + 尺码码    例：X001 01 38  →  X0010138（内部主键）
条码          = CODE128(款号)              例：CODE128("X001")  ← 同款所有色码共用这一条
```

| 组成 | 规则 | 示例 |
| --- | --- | --- |
| 类别字母 | 鞋 X / 服装 F / 裤 K / 配饰 P / 包袋 B / 其他 O（**设置里可自定义**） | 鞋 → `X` |
| 款流水 | 该类别内 3 位递增流水（001–999），**同款共用** | 第 1 款 → `001` |
| 色序 | **同款内按录入顺序**自动编号 2 位（01、02…）；同一颜色复用同一序号 | 白→`01`，黑→`02` |
| 尺码码 | 尺码值清洗：`38`→`38`、`XL`→`XL`、`38.5`→`385`、`39/40`→`39-40`（去空格与非法字符） | 38 码 → `38` |
| 完整 id | 拼接后校验全局唯一，冲突则顺延 | `X001` + `01` + `38` = **X0010138** |

**生成时机与规则**

- 建档保存时生成；**保存前实时预览**编码（页面顶部显示"将生成：款号 X001 / id X0010138"）。
- **同款判定**：名称 + 类别 完全相同 → 复用已有款号，只新增 SKU（色序 +1）；不同 → 新分配款号流水。
- **重复拦截**：同一款下"颜色 + 尺码"已存在 → 提示"该色码已存在（id X0010138）"，不重复生成。
- **可改**：款号允许手动改（改后同步更新该款所有 SKU 的 id 与条码，并提示需重打标签）；id 生成后不可改（避免条码失效），只能作废重建。
- **条码长度**：条码内容 = 款号，固定 4 字符（`X001`），CODE128 约 79 个模块，**40×30mm 标签即可达到 0.44mm 模块宽**，打印清晰、手机好扫。
- **生成即记录**：条码写入款档案（同款所有 SKU 共用），SKU id 写入色码档案，记录"条码来源=系统生成、生成时间"，写入操作日志。

**条码来源**

| 来源 | 场景 | 处理方式 |
| --- | --- | --- |
| A. 系统生成（默认） | 无现成条码 | 按上面规则自动生成 → 打印标签 → 贴到商品上 |
| B. 供应商吊牌现成条码 | 商品自带条码 | 建档时点【扫码录入】扫进来（或手输），覆盖自动生成的条码，并标记来源=供应商吊牌 |

### 11.4 条码生成与打印（手机蓝牙直连热敏标签机）

> **硬件前提（用户已确认）**：**支持蓝牙、可手机直连**的热敏标签机 + 卷装不干胶标签纸（热敏模式，不需要碳带）。
> **主流程（用户已确认）**：手机建档 → 立即蓝牙打印标签 → 贴到商品上；销售时扫码 → 看库存 → 卖。

#### 11.4.1 生成入口

- **手机端建档页（主入口）**：填完新款信息点【保存并打印】→ 自动生成条码并直接蓝牙打印（见 11.9 完整流程）。
- **进货单保存后**：提示"是否打印这批标签"→ 进入矩阵填数页 → 蓝牙打印。
- 商品档案列表：勾选商品 → 【批量生成条码】；无条码商品可一键筛选出来批量生成。
- 商品档案详情页 → 【生成条码】（已有条码时不覆盖，需点【重新生成】才覆盖，覆盖时留日志）。

#### 11.4.2 标签规格与默认参数（按热敏机特性定死）

| 参数 | 默认设置 | 说明 |
| --- | --- | --- |
| 标签尺寸 | **40×30mm（默认）** | 条码内容是 4 字符款号，40mm 宽足够；可选 50×30mm、60×40mm（内容更长时用） |
| 打印分辨率 | **203dpi**（可按机型切 300dpi） | 决定条码位图像素：40×30mm @203dpi ≈ 320×236px |
| 条码类型 | CODE128（内容 = 款号，如 `X001`） | 约 79 模块，40mm 标签下模块宽 ≈0.44mm，远超 0.25mm 下限，热敏打印清晰 |
| 条码高度 | **10mm** | 低于 8mm 手机摄像头不易识别 |
| 左右静区（quiet zone） | **≥2.5mm** | 静区不足是"打印出来扫不出"的头号原因，模板强制留白 |
| 字号 | 商品名 10pt / 款号·售价 9pt / 条码数字 8pt | 热敏纸小于 8pt 容易糊 |
| 打印输出 | **蓝牙直连发送位图指令**（手机端）；电脑端另提供浏览器打印通道 | 见 11.4.3 / 11.4.4 |

**标签内容（自上而下）**：店名（可开关）→ **产品名称** → **颜色 + 号码（尺码）** → **价格** → **条形码** → 条码下方可读数字（即款号，破损时可手输）。

> **同一款：每个"颜色+号码"各打一张标签，但条码完全相同（都是款号 X001），只有文字不同**（白/38、白/39、黑/38…）。顾客手上每双鞋的标签都写明了颜色和尺码，扫任意一个都进同一款，再点对应格子销售。

#### 11.4.3 蓝牙打印实现要点（手机端主通道，开发必读）

1. **蓝牙通道**：浏览器 **Web Bluetooth API**（`navigator.bluetooth.requestDevice`）连接标签机，通过 GATT 写特征发送打印指令。**要求 https 环境**（与摄像头扫码一致，正好由 11.7 的托管方案满足）。
2. **平台限制（重要）**：Web Bluetooth 在 **Android（Chrome/Edge）可用**；**iOS Safari 不支持**（iPhone 上 Chrome 也受 Safari 内核限制）。
   - **iPhone 兜底路径**：① 生成标签 PDF/PNG → 用系统"分享"发给打印机厂商官方 App 打印；② 或加入待打印队列 → 在电脑端一键打印（走 11.4.4 通道）。
   - 界面在检测到不支持蓝牙时**自动隐藏蓝牙打印按钮**，只显示兜底入口，避免点了没反应。
3. **指令适配层**：不同机型指令不同，内置两套常用指令并设置里切换：
   - **TSPL**（TSC 兼容，多数国产标签机：汉印/佳博/芯烨/精臣等）→ 走 `BITMAP` 指令；
   - **ESC/POS**（部分标签机与小票机通用）→ 走 `GS v 0` 位图指令。
   - 首次使用必须**先打测试页**选对指令集（打出来是乱码就换另一个），选择结果本地保存。
4. **位图生成**：标签先渲染成 **1-bit 黑白位图**，按 DPI 精确计算像素（40×30mm @203dpi = 320×236px），**禁止任何缩放/拉伸**；条码模块宽 ≥3px。
5. **连接体验**：首次点【连接打印机】→ 系统弹窗选设备（需用户手势触发）→ 记住设备，下次进入自动重连提示；断连时给出"重连/改用电脑打印"入口。
6. **先打测试页**：提供【打印测试标签】（固定条码 `TEST001`），**手机扫通了才允许批量打印**，避免整卷浪费。

#### 11.4.4 电脑端打印通道（备用 / iPhone 兜底）

- 浏览器标准打印：`@page { size: Wmm Hmm; margin: 0 }`，一页一张标签，打印 N 张输出 N 页，**强制 100% 缩放**（关掉"适合页宽"）。
- 打印前提示：驱动里设好纸张尺寸、选对打印机、关页眉页脚；打印浓度与走纸校准在驱动里做（长按 FEED 键测纸）。
- 提供【导出 PNG / PDF】（按 203/300dpi），供厂商软件或第三方排版打印。
- 手机端生成的**待打印队列**同步到电脑后，电脑端一键批量打印（队列内容存在备份 JSON 中，导入即可见）。

#### 11.4.5 打印数量与记录

- **手机建档后**：默认按每个色码的**进货数量**填张数（进 3 双白/38 就打 3 张），可在矩阵里逐个改（见 11.9）。
- 商品档案里补打：默认每款 1 张；可按"当前库存件数"一键填入，支持手动改与批量多款。
- 打印记录：记录打印时间、张数、对应商品与色码、模板尺寸；列表支持"已打印 / 未打印"筛选，避免漏打或重复打。

#### 11.4.6 耗材建议

- 用**热敏不干胶标签纸**（不需要碳带）；贴在鞋盒/吊牌上长期保存建议选**三防热敏纸**（防水防油防刮），普通热敏纸遇热或日晒会变黑、条码失效。
- 跨季仓储建议：40×30mm 三防热敏纸 + CODE128。

### 11.9 完整主流程：采购到货 → 手机建档 → 打标签 → 扫码销售

```
① 手机【新款建档】——只填 4 项
   名称 / 类别 / 颜色（可多选）/ 号码（可多选）＋ 进价 / 售价
   ↓【保存并打印】
② 系统自动生成（保存前实时预览）
   款号 X001（同款共用）→ 各色码 SKU id：X0010138、X0010139、X0010238…
   → 条码 = CODE128(X001)，同款所有色码共用这一条
   ↓
③ 弹出"标签打印"页：每个色码一行，默认张数 = 该色码进货数量（可逐行改）
   ↓ 蓝牙已连接 →【打印】
④ 标签逐张打出：产品名 / 颜色+号码 / 价格 / 条码
   （条码都一样，文字不同 → 分别贴在白/38、白/39、黑/38…上）
   ↓ 贴到鞋盒或吊牌上
⑤ 销售时：开单页点📷扫吊牌条码
   → 1 秒内弹出该款商品卡：名称、售价、颜色×尺码库存矩阵
   → 点对应「颜色+尺码」格子 → 加入销售单 → 选收款方式 → 保存
   → 库存自动 −1，收入与流水自动记账
```

- **时间目标**：填 4 项到打出第一张标签 ≤ 90 秒；扫码到加入销售单 ≤ 3 秒。
- 若这批货同时要入库：在①之前或之后补一张进货单即可（进货单保存后同样会提示打印这批标签）。
- 卡片下方仍可点【查看整款矩阵】看同款所有色码库存，方便给顾客换码。

### 11.5 扫码入口（4 个）

| 入口 | 位置 | 行为 |
| --- | --- | --- |
| 扫码查库存 | 库存页 / 首页主按钮旁 📷 | 扫 → 弹出该款商品卡（售价、颜色、尺码、库存矩阵），点格子可加入销售单或去盘点 |
| 扫码销售 | 开单页搜索框右侧 📷 | 连续扫码模式：扫条码 → 弹该款矩阵 → 点色码加入明细；重复扫同一条码 → 已加入的行数量累加；带提示音/高亮反馈 |
| 扫码入库 / 盘点 | 进货明细、盘点页 | 扫 → 弹该款矩阵 → 点色码 +1；未建档提示先建档 |
| 扫码建档 | 商品档案 → 新建 → 条码框 📷 | 扫现成吊牌条码自动填入，避免手工输错 |

### 11.6 识别方式（三级降级，保证任何环境都能扫）

1. **实时预览扫码**（首选）：调用摄像头连续识别，体验最好，支持连续扫。**注意：浏览器要求"安全上下文"，即必须 https:// 或 localhost 才能打开摄像头。**
2. **拍照识别**（兜底，离线可用）：通过系统相机拍一张照片后本地解码，**不要求 https**，本地文件双击打开（file://）也能用。
3. **手写输入**：直接输入条码数字，摄像头不可用/光线差/条码破损时兜底。

> 电脑端 USB 扫码枪属键盘模拟输入，天然可用，不需要任何摄像头权限。
> 条码**生成与打印只在电脑端做**（要接打印机），**扫码使用主要在手机端**，两端数据一致。

### 11.7 部署方式调整（因为扫码，需要微调）

手机摄像头要能用，必须让手机通过 **https** 访问页面。可选方案（均不买服务器，数据仍存在手机本地）：

- **方案 A（推荐）**：把应用（纯静态文件）托管到**免费静态网站服务**（如 EdgeOne Pages / GitHub Pages / Cloudflare Pages / 腾讯云 COS 静态网站），手机用 https 网址打开并"添加到主屏幕"，配合 Service Worker 缓存后**离线仍可打开使用**；商业数据全部留在手机本地，服务端不存任何数据。
- **方案 B**：电脑在同一局域网起一个本地服务，手机通过 https 隧道访问（体验不稳定，仅临时演示用）。
- **方案 C**：只用 file:// 本地文件打开——此时**实时扫码不可用，自动降级为"拍照识别 + 手写输入"**，其余功能（含电脑端生成打印条码）不受影响。

> 结论：推荐**方案 A + 拍照识别兜底**：平时手机用 https 网址实时扫码，网络异常或离线时仍可拍照识别和手写输入，核心功能不中断。

### 11.8 未识别 / 异常条码处理

- 未建档 → 提示"未找到此条码"，提供【立即建档】，跳转新建商品并预填条码（若条码符合自动编码格式，自动反填类别）。
- 条码重复 → 建档/导入/生成时拦截并指出已被哪个款占用。
- 该款全部色码库存为 0 → 提示"此款已无库存"；单个色码为 0 → 矩阵中灰显不可点。
- 已停售商品 → 查库存可看，销售扫码时提示"该商品已停售，是否继续"。
- 条码破损识别失败 → 提示"识别失败"，提供【重试】【手输条码】两个按钮。

---

*本 PRD 为开发唯一依据；实施顺序按 PRD 第 8 节功能对应实施方案阶段 1→2→3 推进，每阶段完成即按第 10 节对应条目验收。需求变更请先更新本文档。*
