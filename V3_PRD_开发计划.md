# V3 版本开发文档与计划（多店铺多账号版）

> 版本：V3 规划稿（PRD + 开发计划）· 2026-09-01
> 状态：待确认后进入开发
> 关联：V2-2 固定版（e952c4d，已发布）

---

## 1. 项目背景与目标

当前系统（V2-2）是**单店单账号**进销存记账工具：本地单库存储、本地锁屏密码、单一 GitHub Pages 云同步。

用户现有 **3 家店铺**，未来可能扩张到 **十几家**。V3 目标：让**每家店铺使用独立账号登录**本系统，账号间**数据完全隔离**，同时保留**各自独立的 GitHub 云同步**能力，并允许每个账号**自定义头像与店铺名称**。

### V3 总体目标

| 目标 | 说明 |
|---|---|
| 多账号登录 | 每个店铺一个账号，登录进入各自的经营空间 |
| 经营范围隔离 | 账号1=鞋子、账号2=服装、账号3=饰品，其他账号暂未分配（保留） |
| 账号个性化 | 每个账号可自行更改头像、店铺名称 |
| 数据独立 | 各账号数据互不共有、互不干扰 |
| 同步独立 | 各账号可分别同步到 GitHub，互不影响 |

---

## 2. 需求详细拆解

### 需求1：多店铺多账号登录 + 经营范围

- 系统启动进入**账号登录页**（代替现有单账号直接进入）
- 预置账号：
  - 账号1 → 经营范围：**鞋**（款号前缀 X）
  - 账号2 → 经营范围：**服装**（款号前缀 F）
  - 账号3 → 经营范围：**饰品**（款号前缀 P，映射现有"配饰"分类）
  - 其他账号 → 未分配经营范围（保留、可后续分配）
- 登录后业务约束：
  - **开单/选货/商品建档**等商品维度操作，只显示本账号经营范围对应的分类与款号
  - 首页统计、库存、报表均基于本账号数据

### 需求2：账号自定义头像与店铺名称

- 「我的」页店铺卡片：可编辑**店铺名称**（已有 shopName 机制扩展）、**上传头像**
- 头像保存后全局生效（顶栏头像、侧栏、首页 banner、登录页）
- 店名与头像只作用于当前账号，不影响其他账号

### 需求3：账号数据独立

- 每个账号拥有**完全独立的数据存储**
- 切账号 = 切数据空间，互不可见、互不影响
- 首次进入某账号自动初始化该账号的空账本

### 需求4：账号独立 GitHub 同步

- 每个账号可单独配置并执行云同步（上传/恢复）
- 各账号同步目标**互不干扰**（独立存储路径 / 独立密钥 / 独立配置）
- 沿用现有"加密信封 → GitHub Contents API"机制

---

## 3. 现有系统架构分析（V2 现状）

| 模块 | 现状 | V3 影响 |
|---|---|---|
| 数据存储 | IndexedDB 单库 `shoeErp`（v1），11 个 store（products/skus/purchases/sales/stocktakes/stockLogs/ledgers/partners/printJobs/logs）+ meta(settings) | 需支持**每账号独立库** |
| 登录 | 仅本地锁屏密码（`settings.lock.hash`），无账号概念 | 需新增**账号体系 + 登录页** |
| 设置 | `settings.shopName`（店名）、`categoryPrefix`（款号前缀）、`lock` 等 | 扩展 **avatar 头像字段 + 经营范围字段** |
| 商品分类 | `CATEGORIES: ['鞋','服装','裤','配饰','包袋','其他']` | 经营范围映射到分类子集 |
| GitHub 同步 | `core/sync.js`：快照 JSON → AES-GCM 加密 → GitHub Contents API → 固定路径 `data/erp-snapshot.json`；配置存 `localStorage['erp.sync.config']` | 需**按账号参数化**（独立路径/独立配置 key/独立口令） |
| 我的页 | 店铺卡片（头像 emoji/logo + 店名）+ 云同步卡片 + 快捷入口 | 增加**头像上传、账号切换、经营范围展示** |
| 测试 | 44 个测试文件，全量 390 用例/386 通过（4 个 jsdom 预存失败） | 新增账号/隔离/经营范围/同步隔离测试 |

---

## 4. 关键设计决策（方案对比，待确认）

### 决策D1：账号体系形态

| 方案 | 描述 | 优点 | 缺点 |
|---|---|---|---|
| **A（推荐）** | **本地账号**：账号列表存本机，登录=账号+密码（密码沿用现有 lock 加密），无联网注册 | 无服务器、离线可用、复用现有密码机制 | 账号仅本机有效，换设备需手动迁移 |
| B | GitHub 账号 OAuth 登录 | 跨设备一致 | 需 OAuth 流程，纯前端复杂度高，不适合当前架构 |

**倾向 A**：符合"每个店铺都登录这个系统"且保持无服务器。

### 决策D2：账号数据隔离方案（核心）

| 方案 | 描述 | 优点 | 缺点 |
|---|---|---|---|
| **A（推荐）** | **每账号独立 IndexedDB 库**：`shoeErp_<acctId>`，账号切换=切换数据库 | 物理隔离最彻底；现有 db/repo 全部逻辑只需参数化 dbName，改动小、风险低；备份/同步天然按账号 | 同设备多账号各占一份存储 |
| B | 单库多账号（数据表加 accountId 列） | 单库统一 | 所有查询/统计/单据关联全要改，风险极高 |
| C | JSON 文件级隔离 | 结构简单 | 性能差，多 store 关系维护复杂 |

**倾向 A**：每账号独立库，物理隔离、改动面最小。

### 决策D3：账号切换与登录形态

- 登录页列出本机已有账号（头像 + 店名 + 经营范围标签），点击 → 输密码 → 进入
- 「我的」页提供**切换账号**入口（退出回登录页）
- 首次使用：初始化默认账号或引导创建账号（V3 预置 3 账号 + 保留位）

### 决策D4：经营范围实现

- `settings` 新增 `scopeCategories: ['鞋']`（本账号允许的分类列表）
- 商品建档/开单/选货/库存展示：按 `scopeCategories` 过滤
- 款号前缀规则沿用 `categoryPrefix`（鞋X/服装F/配饰P）——开单时仅生成本经营范围的款号前缀
- 未分配账号（暂未分配经营范围）：默认开放全部分类，保留后续收紧能力

### 决策D5：GitHub 同步隔离

| 方案 | 描述 |
|---|---|
| **A（推荐）** | **独立存储路径**：`data/<acctId>/erp-snapshot.json`（现有 sync 已支持 path 参数化）；每账号独立口令 + 独立配置 key（`erp.sync.config.<acctId>`） |
| B | 独立分支：每账号一个 branch（如 `gh-pages-<acctId>`） |

**倾向 A**：路径隔离最轻量，改动集中在 sync 配置参数化。

### 决策D6：头像存储

- 头像图片经 FileReader → dataURL（限制 ≤ 512KB），存入当前账号 `meta` 的 `settings.avatar`
- 各界面（登录页/我的/顶栏/侧栏/banner）优先读 `settings.avatar`，无则回退现有 logo/emoji

---

## 5. 技术方案总览

```
V3 架构：
┌─ 登录页（账号列表 + 密码）─────────────────────────┐
│  账号1 鞋店(鞋) │ 账号2 服装店(服装) │ 账号3 饰品店(饰品) │ 保留账号... │
└──────────────┬───────────────────────────────────┘
               ▼ 登录（校验密码，切换数据空间）
┌─ 当前账号上下文 ctx.account ────────────────────────┐
│  IndexedDB: shoeErp_<acctId>（独立库）               │
│  settings: shopName / avatar / scopeCategories       │
│  localStorage: erp.sync.config.<acctId>（独立同步配置）│
└───────────────────────────────────────────────────┘
  业务层：开单/库存/报表 —— 按 scopeCategories 过滤
  同步层：sync.js —— path=data/<acctId>/erp-snapshot.json
```

### 涉及文件

| 文件 | 改动 |
|---|---|
| `js/core/schema.js` | settings 增加 `avatar`、`scopeCategories`；DB 命名改为按账号 |
| `js/store/db.js` / `js/store/repo.js` | dbName 参数化（`shoeErp_<acctId>`） |
| `js/core/sync.js` | 配置 key 按账号；path 默认值按账号 |
| `js/app.js` | 启动流程：账号选择 → 密码校验 → 按账号初始化 ctx/db |
| `js/ui/page-login.js`（新） | 登录页（账号列表、密码、新建/切换） |
| `js/ui/page-mine.js` | 头像上传、店名编辑、经营范围展示、切换账号 |
| `js/ui/*`（home/product/sale/inventory 等） | 按 scopeCategories 过滤商品/分类 |
| `index.html` | 引入 page-login.js；banner/顶栏读账号头像店名 |
| 测试 | 新增 account/scope/sync-isolation 测试文件 |

---

## 6. 详细开发计划

> 遵循项目惯例：**一次只解决一个功能点 → 添加并运行对应测试（不删不跳）→ 测试通过 → 单独 Git 提交**；全部完成后运行全量测试验收；敏感信息审查后提交/部署。

### 阶段 0：需求与设计确认（本轮）
- [x] 需求梳理、现状分析
- [x] 关键决策点 D1–D6（本文档）——**需用户确认**
- [ ] 确认后冻结设计，进入开发

### 阶段 1：账号体系与登录（基础）
| # | 任务 | 验收 | 测试 |
|---|---|---|---|
| 1.1 | 新增 `core/accounts.js`：账号注册/列表/密码校验（复用 PBKDF2/AES 密码机制） | 可创建/校验账号 | accounts.test.js |
| 1.2 | 新增登录页 `ui/page-login.js`：账号列表 + 密码 + 进入 | 登录页可展示预置账号并进入 | ui-login.test.js |
| 1.3 | 预置 3 账号（鞋/服装/饰品）+ 保留位，初始化各自 settings.scopeCategories | 各账号经营范围配置正确 | accounts.test.js |

### 阶段 2：数据隔离（按账号独立库）
| # | 任务 | 验收 | 测试 |
|---|---|---|---|
| 2.1 | db/repo 支持 dbName 按账号（`shoeErp_<acctId>`） | 切换账号加载不同库 | db-account.test.js |
| 2.2 | app 启动按登录账号初始化 ctx（账号 → 独立库 → 独立 settings） | 两账号数据互不可见 | e2e-account.test.js |

### 阶段 3：经营范围过滤
| # | 任务 | 验收 | 测试 |
|---|---|---|---|
| 3.1 | 商品建档/开单/选货/库存/报表按 scopeCategories 过滤 | 鞋店只出"鞋"，服装店只出"服装" | scope.test.js |
| 3.2 | 款号前缀按账号经营范围（X/F/P） | 各账号生成对应前缀款号 | scope.test.js |

### 阶段 4：账号个性化（头像/店名）
| # | 任务 | 验收 | 测试 |
|---|---|---|---|
| 4.1 | settings 增加 avatar（dataURL），「我的」页上传头像 | 上传后全局头像更新 | mine-avatar.test.js |
| 4.2 | 店名编辑（复用现有 shopName）+ 头像在登录页/顶栏/侧栏/banner 生效 | 各界面展示账号自定义头像店名 | mine-avatar.test.js |

### 阶段 5：同步隔离
| # | 任务 | 验收 | 测试 |
|---|---|---|---|
| 5.1 | sync 配置 key 按账号（`erp.sync.config.<acctId>`）+ path 按账号（`data/<acctId>/erp-snapshot.json`） | 两账号同步互不覆盖 | sync-account.test.js |
| 5.2 | 「我的」页云同步卡片绑定当前账号配置 | 各账号独立同步/恢复 | sync-account.test.js |

### 阶段 6：收尾与发布
| # | 任务 | 验收 |
|---|---|---|
| 6.1 | 全量测试（含全部新旧测试） | 仅保留 jsdom 4 个预存失败 |
| 6.2 | README 更新 V3 说明 + 敏感信息审查 | 无 AK/密钥/隐私泄漏 |
| 6.3 | Git 提交 + 打 V3 标签 + 推 gh-pages 发布 | 线上可登录多账号 |

---

## 7. 风险与依赖

| 风险/依赖 | 说明 | 应对 |
|---|---|---|
| 存量单账号数据迁移 | V2 已有数据在 `shoeErp` 单库，升级后归属哪个账号？ | 默认迁移到"账号1"或提供迁移入口（待确认） |
| 同步兼容 | V2 旧同步 path `data/erp-snapshot.json` 不再使用 | V3 各账号用新 path，旧数据可手动恢复 |
| 头像体积 | dataURL 可能较大 | 限制 ≤512KB，超限压缩/拒绝 |
| 浏览器离线 | 登录/多库均本地，无新增网络依赖 | 保持无服务器 |
| 多账号切换丢失状态 | 切账号需重置页面状态 | 登录/退出时清空 pageStates 并刷新 |

---

## 8. 关键决策确认记录（2026-09-01 已确认）

| # | 决策项 | 确认结果 |
|---|---|---|
| 1 | 账号形态 | ✅ 本地账号 + 密码（无服务器） |
| 2 | 数据隔离 | ✅ 每账号独立 IndexedDB 库（`shoeErp_<acctId>`） |
| 3 | 存量数据归属 | ✅ V2 现有单账号数据迁移到账号 1 |
| 4 | 初始密码 | ✅ 预置 3 账号初始密码 = `000000`（登录后可改） |
| 5 | "饰品"映射 | ✅ 账号 3 经营范围直接映射现有「配饰」分类 |
| 6 | 未分配账号 | ✅ 支持自行创建登录账户与密码，**最多 10 个账号** |

> 设计更新：未分配账号改为"可自行注册"（非只读保留位），自建账号默认全部分类开放（未分配经营范围，后续可收紧）。
