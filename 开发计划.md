# 鞋服店进销存记账软件 · 开发计划（可直接执行版）

- 依据文档：`PRD.md`（V1.x，需求唯一来源）、`鞋服店进销存记账软件-实施方案.html`
- 计划版本：V1.0　｜　日期：2026-08-31
- 目标：16 个工作日内交付第一版全部功能（PRD 第 8 节），逐 Sprint 可运行、可验收

---

## 1. 开发原则（先立规矩，避免返工）

| 原则 | 具体约束 |
| --- | --- |
| 零构建、零 npm 依赖 | 不装任何包、不打包。所有第三方能力以 `vendor/*.js` 本地文件随包发布，离线可用 |
| 双击可用 | **不使用 ES Module**（`<script type="module">` 在 `file://` 下会被 CORS 拦截）。统一用经典 `<script>` 按序加载 + 全局命名空间 `window.ERP` |
| 逻辑与界面分离 | `core/` 全是纯函数（不碰 DOM、不碰 IndexedDB），可在 Node 里直接测；`ui/` 只做渲染与事件 |
| 单据是唯一事实来源 | 库存、流水、欠款全部由单据派生，禁止任何"手改余额"的入口（PRD 6） |
| 每个 Sprint 可验收 | 每个 Sprint 结束必须能真机跑通，并勾掉 PRD 第 10 节对应条目 |
| 先跑通再优化 | 第一版不做抽象过度设计；PRD 第 9 节明确不做的功能一律不碰 |

---

## 2. 目录结构与文件清单

```
erp/
├── index.html                  # 唯一入口（单页应用外壳）
├── css/
│   ├── base.css                # 变量、重置、通用组件（按钮/卡片/表格/表单）
│   ├── mobile.css              # <768px：底部 Tab、单列
│   ├── desktop.css             # ≥768px：左侧栏、双列
│   └── print.css               # 标签打印页（@page 40×30mm、margin:0）
├── js/
│   ├── core/                   # 纯逻辑层（可 Node 测试，不碰 DOM）
│   │   ├── util.js             # 金额/日期/ID/编码清洗工具
│   │   ├── schema.js           # 数据结构定义 + schemaVersion 迁移
│   │   ├── coding.js           # ★ 款号 / SKU id / 条码自动生成（PRD 11.3）
│   │   ├── inventory.js        # ★ 库存增减、矩阵、预警、盘点差异
│   │   ├── docNo.js            # 单据号生成 P/S/T 规则
│   │   ├── ledger.js           # 收支流水生成（自动 + 手工）
│   │   ├── debt.js             # 往来单位应收/应付余额与还款冲减
│   │   ├── profit.js           # ★ 三层利润口径（PRD 5.7）
│   │   ├── backup.js           # 备份 JSON 组装/校验/恢复（PRD 7）
│   │   └── barcode.js          # ★ CODE128 编码（条空序列 + 校验位）
│   ├── store/
│   │   └── db.js               # IndexedDB 封装（open/get/put/query/tx）
│   ├── ui/
│   │   ├── router.js           # hash 路由 + 断点切换布局
│   │   ├── components.js       # 通用组件：Toast/Modal/矩阵表格/分页/搜索栏
│   │   ├── page-home.js        # 首页经营看板（PRD 3）
│   │   ├── page-product.js     # 商品档案 + 建档（PRD 5.1）
│   │   ├── page-purchase.js    # 进货管理（PRD 5.2）
│   │   ├── page-sale.js        # 销售开单 + 赠送 + 退货（PRD 5.3/5.4）
│   │   ├── page-inventory.js   # 库存查询 + 预警 + 盘点（PRD 5.5）
│   │   ├── page-account.js     # 记账中心（PRD 5.6）
│   │   ├── page-report.js      # 报表与利润（PRD 5.7）
│   │   └── page-setting.js     # 备份恢复/密码/打印设置（PRD 5.8）
│   ├── barcode/
│   │   ├── render.js           # 条码 → SVG / Canvas 位图（按 DPI）
│   │   ├── label.js            # 标签版式渲染（40×30mm）+ 打印页生成
│   │   ├── scan.js             # 扫码三级降级：实时 / 拍照 / 手输（PRD 11.6）
│   │   └── print-bt.js         # 蓝牙打印：Web Bluetooth + TSPL / ESC-POS
│   └── app.js                  # 启动、初始化、Tab/侧栏挂载
├── vendor/
│   └── zxing.min.js            # 一维码解码（仅在点扫码时懒加载）
├── tests/
│   ├── coding.test.js          # 编码生成
│   ├── inventory.test.js       # 库存联动
│   ├── profit.test.js          # 利润口径
│   ├── backup.test.js          # 备份恢复
│   └── barcode.test.js         # CODE128 编码与校验位
├── sw.js                       # Service Worker（离线缓存外壳，仅 https 托管时生效）
├── manifest.webmanifest        # PWA：添加到主屏幕
└── README.md                   # 运行方式、备份纪律
```

> **core 文件同时兼容浏览器与 Node**（文件末尾加一段导出判断）：
> ```js
> if (typeof module !== 'undefined' && module.exports) module.exports = ERP.coding;
> ```
> 这样 `node --test tests/` 可以直接 `require('../js/core/coding.js')`。

---

## 3. 数据层设计

### 3.1 IndexedDB 对象仓库（库名 `shoeErp`，版本 1）

| 仓库 | 主键 | 说明 |
| --- | --- | --- |
| `products` | `styleCode` | 款（款号、名称、类别、进价、售价、条码、状态） |
| `skus` | `id` | 色码（id、款号、颜色、尺码、库存、阈值） |
| `purchases` | `no` | 进货单 |
| `sales` | `no` | 销售单（含赠送、退货） |
| `stockLogs` | `id` | 库存变动流水（可追溯） |
| `ledgers` | `id` | 收支流水 |
| `partners` | `id` | 往来单位（供应商/客户） |
| `meta` | `key` | schemaVersion、设置、密码哈希、最后备份时间、打印配置 |

### 3.2 核心数据结构（照抄即可）

```js
// 款
{ styleCode:'X001', name:'小白鞋', category:'鞋', brand:'',
  costPrice:50, salePrice:129, barcode:'X001', barcodeSource:'system',
  createdAt:'2026-08-31T10:00:00', status:'on' }

// 色码 SKU
{ id:'X0010138', styleCode:'X001', color:'白', size:'38',
  stock:3, threshold:3, status:'on' }

// 销售单（类型：sale 销售 / gift 赠送 / refund 退货）
{ no:'S20260831-001', date:'2026-08-31', partnerId:null,
  items:[ { skuId:'X0010138', styleCode:'X001', color:'白', size:'38',
            qty:1, price:129, costSnapshot:50, type:'sale', giftReason:null } ],
  discount:0, payments:[{method:'wechat', amount:129}],
  received:129, debt:0, createdAt:'' }

// 库存变动流水
{ id:'...', date:'', skuId:'X0010138', delta:-1, balance:2,
  refType:'sale', refNo:'S20260831-001' }
```

### 3.3 关键约定

- **成本快照**：销售单每个 item 保存 `costSnapshot`（当时的进价），历史利润永不因调价变化（PRD 10.2）。
- **单据只作废不删除**：`voided:true`，作废时反向回滚库存/流水/欠款并写 `stockLogs`。
- **金额一律整数分存储，显示时除 100**，避免浮点误差。

---

## 4. 核心算法规格（实现时严格照此）

### 4.1 编码生成 `core/coding.js`（PRD 11.3）

```js
genStyleCode(category, existing)      // '鞋' → 'X001'（类别内 3 位流水 +1）
genSkuId(styleCode, colorSeq, size)   // 'X001' + '01' + '38' → 'X0010138'
cleanSize('38.5') → '385'             // '39/40' → '39-40'，去空格
nextColorSeq(styleCode, color)        // 同色复用序号，新色 +1
genBarcode(styleCode) → 'X001'        // 条码内容 = 款号（同款共用）
```

规则：同款判定 = `name + category` 完全相同 → 复用款号；否则新开流水。id 冲突顺延。

### 4.2 库存 `core/inventory.js`

```js
applyPurchase(purchase)  // 每个 item：sku.stock += qty，写 stockLogs(+)
applySale(sale)          // sale/gift：sku.stock -= qty；refund：+= qty，写 stockLogs(-)
buildMatrix(styleCode)   // { colors:[], sizes:[], cells:{'白|38':3} }
getAlerts(threshold)     // 低于阈值的 SKU 列表
applyStocktake(diffs)    // 盘点：差异写 stocktake 单并留痕
```

### 4.3 利润 `core/profit.js`（PRD 5.7）

```
销售收入 = Σ(price×qty) − 退货
销售成本 = Σ(costSnapshot×qty)
毛利     = 收入 − 成本
赠送成本 = Σ(赠品 costSnapshot×qty)（单列，不计入销售成本）
净利参考 = 毛利 − 赠送成本 − 费用支出
库存资金占用 = Σ(当前库存 × 最新进价)
```

### 4.4 条码与打印 `core/barcode.js` + `barcode/`

- `code128Encode(text)` → 条空模块数组 + 校验位（自实现，约 120 行，CODE128-B 子集足够）
- `renderToCanvas(text, {dpi, moduleMm, heightMm, quietMm})` → 位图；模块宽 = 可用宽 / 模块数，目标 ≥0.375mm，下限 0.25mm
- `renderLabel(sku)` → 40×30mm 版式：店名 / 产品名 / 颜色+号码 / 价格 / 条码 / 款号数字
- `toBitmap1bpp(canvas)` → 逐行打包字节（供 TSPL `BITMAP` / ESC-POS `GS v 0`）
- `printViaBT(pages)` → Web Bluetooth 写特征，按选择发 TSPL 或 ESC-POS 指令

### 4.5 扫码三级降级 `barcode/scan.js`（PRD 11.6）

1. `BarcodeDetector`（Android Chrome 原生，优先）
2. 拍照识别：`<input type="file" accept="image/*" capture="environment">` → 懒加载 `vendor/zxing.min.js` 解码（file:// 也能用）
3. 手输条码数字（等价于扫码结果）

---

## 5. Sprint 计划（共 16 个工作日）

> 每个 Sprint 开头先跑 `node --test tests/` 保证既有逻辑不回归；结尾按"完成标准"真机验收。

### Sprint 0 · 工程骨架与存储层（1 天）

**任务**
- [ ] 建目录结构，写 `index.html` 外壳（顶部标题区 + 内容容器 + 底部 Tab/侧栏占位）
- [ ] `css/base.css`：CSS 变量（蓝/绿/橙/红主题色）、按钮、卡片、表格、表单、Toast、Modal
- [ ] `store/db.js`：IndexedDB 封装 `open()/get(store,key)/put()/query(store,index,range)/bulkPut()/tx()`
- [ ] `core/schema.js`：数据结构常量、默认设置、`schemaVersion=1` 与迁移钩子
- [ ] `core/util.js`：金额分制换算、日期格式化、`uuid()`、编码清洗
- [ ] `js/app.js`：启动流程（打开库 → 载入设置 → 挂载路由）
- [ ] `tests/` 建 5 个测试文件骨架，`node --test tests/` 跑通（空用例）

**完成标准**：`file://` 双击 `index.html` 能打开不报错；IndexedDB 建库成功；`node --test` 通过。

---

### Sprint 1 · 编码生成 + 商品档案（2 天）★ 核心

**任务**
- [ ] `core/coding.js`：款号 / SKU id / 条码生成 + 同款判定 + 色序 + 尺码清洗 + 重复拦截
- [ ] `tests/coding.test.js`：覆盖 PRD 10.0 全部 9 条（X001 / X0010139 / X0010238 / 尺码清洗 / 重复拦截 / 改款号同步 / 多色多码批量）
- [ ] `ui/page-product.js` 建档页（手机优先）：
  - 只填 4 项：**名称 / 类别 / 颜色（多选）/ 号码（多选）** + 进价 / 售价
  - 保存前**实时预览**"将生成：款号 X001 / 条码 CODE128(X001)"
  - 一次勾选 2 色 × 3 码 → 批量生成 6 个 SKU
- [ ] 商品档案列表：搜索（名称/款号/条码）、分页、编辑、停售、"有无条码"筛选
- [ ] CSV 批量导入存量商品（含表头映射与错误行提示）

**完成标准**：PRD 10.0 全绿；手机上 60 秒内完成一款 2 色 3 码的建档并看到 6 个 SKU。

---

### Sprint 2 · 进货 + 库存 + 盘点（2 天）

**任务**
- [ ] `core/docNo.js`：单号 `P20260831-001` / `S20260831-001`（按日重置）
- [ ] `core/inventory.js`：`applyPurchase` / `buildMatrix` / `getAlerts` / `applyStocktake`
- [ ] `tests/inventory.test.js`：进货 +3、销售 −2 后库存为 1；作废回滚；盘点差异留痕
- [ ] `ui/page-purchase.js`：进货单录入（选择供应商 → 色码矩阵批量填数）、修改、作废、查询
- [ ] `ui/page-inventory.js`：库存查询（按款/色/码）、**颜色×尺码矩阵**、预警列表、盘点页

**完成标准**：PRD 10.1 前 1 条 + 10.3 全绿；进货保存后库存与流水同步。

---

### Sprint 3 · 销售开单 + 赠送 + 退货（2 天）

**任务**
- [ ] `ui/page-sale.js`：开单页（搜索选品 → 色码 → 数量）、单行改价、整单折扣、组合收款（微信/现金/支付宝/欠款）
- [ ] 赠送：明细行一键切换类型 + 原因（赠品/样品/自用/破损），不计收入、单独记成本
- [ ] 退货：对已保存单据红冲生成退货单（库存 +N、收入冲减）
- [ ] `core/inventory.js#applySale` 联动：库存 −N、写 stockLogs、写销售流水
- [ ] 销售记录列表（按日期/类型筛选、查看、作废留痕）

**完成标准**：PRD 10.1 全绿（含混合赠送单、退货红冲）；手机开单 ≤20 秒。

---

### Sprint 4 · 记账中心 + 欠款（1.5 天）

**任务**
- [ ] `core/ledger.js`：单据自动生成流水 + 手工费用记账（房租/水电/人工/物流/其他）
- [ ] `core/debt.js`：供应商应付、客户应收、付款/收款登记、余额自动冲减
- [ ] `ui/page-account.js`：流水查询（日期/类型）、应付清单 + 付款、应收清单 + 收款
- [ ] 首页"待处理事项条"接入欠款超期提醒（>15 天）

**完成标准**：PRD 10.1 中进货欠款、客户挂账两条全绿。

---

### Sprint 5 · 报表与利润（1.5 天）

**任务**
- [ ] `core/profit.js`：日/月/年口径 + 三层利润 + 库存资金占用 + 畅销/滞销 TOP5
- [ ] `tests/profit.test.js`：用 PRD 10.2 的固定数据验证（收入 1290 / 成本 500 / 毛利 790 / 赠送 50 / 净利 240）
- [ ] `ui/page-report.js`：报表页、趋势图（自绘 SVG，不引图表库）、CSV 导出

**完成标准**：PRD 10.2 全绿；改进价后历史报表数字不变。

---

### Sprint 6 · 扫码（三级降级）+ 商品卡（2 天）

**任务**
- [ ] `barcode/scan.js`：① BarcodeDetector 实时预览（连续扫）② 拍照识别（懒加载 vendor/zxing）③ 手输
- [ ] 商品卡组件：售价 + **颜色×尺码库存矩阵** + 汇总 + 点格子加入销售单（0 库存灰显）
- [ ] 接入 4 个入口：库存页 / 首页 / 开单页 / 进货盘点页（PRD 11.5）
- [ ] 异常：未建档 →【立即建档】预填条码；整款 0 库存提示；识别失败 → 重试/手输

**完成标准**：PRD 10.6 全绿；连扫 20 个条码成功 ≥19 次；扫码到加入销售单 ≤3 秒。

---

### Sprint 7 · 条码渲染 + 蓝牙打印（2 天）★ 风险最高，提前做

**任务**
- [ ] `core/barcode.js`：CODE128-B 编码 + 校验位；`tests/barcode.test.js` 验证条空序列与校验位
- [ ] `barcode/render.js`：按 DPI 渲染 SVG/Canvas，模块宽自适应（≥0.375mm 目标，下限 0.25mm），静区 ≥2.5mm
- [ ] `barcode/label.js`：40×30mm 标签版式（店名/产品名/颜色+号码/价格/条码/款号数字）+ 打印页 N 张
- [ ] `barcode/print-bt.js`：Web Bluetooth 连接 + TSPL / ESC-POS 两套指令 + 位图打包 + **测试页 TEST001**
- [ ] 不支持蓝牙时自动隐藏按钮，显示"分享给 App / 电脑端打印"兜底
- [ ] `css/print.css` + 电脑端 `window.print()` 通道 + 导出 PNG

**完成标准**：PRD 10.7 全绿（含测试页可扫、实打 5 张逐张可扫、连续 20 张不错位）。
**依赖外部输入**：打印机品牌型号（决定默认指令集）、手机是 Android 还是 iPhone。

---

### Sprint 8 · 首页看板 + 备份恢复 + 双端适配（2 天）

**任务**
- [ ] `ui/page-home.js`：今日概览（销售额/销量/毛利/预警数）、主按钮、快捷入口、待办条、备份提醒条；电脑端加月度卡 + 趋势图 + TOP5
- [ ] `core/backup.js`：导出单 JSON（含全部仓库 + schemaVersion + 时间 + 计数摘要）、导入校验 + 覆盖二次确认 + 恢复核对
- [ ] `tests/backup.test.js`：导出→清空→恢复，数据一致；损坏文件报错不破坏现有数据
- [ ] `css/mobile.css` / `desktop.css`：768px 断点、底部 Tab / 左侧栏切换
- [ ] 打开密码（密码哈希存 localStorage）、操作日志
- [ ] `manifest.webmanifest` + `sw.js`（https 托管时离线可用）

**完成标准**：PRD 10.4、10.5 全绿；375px 与 1280px 两种尺寸核心流程跑通。

---

### Sprint 9 · 性能与总验收（1 天）

- [ ] 造 5000 条单据压测：列表分页 <1 秒、开单搜索无卡顿（必要时加索引与虚拟滚动）
- [ ] 断网（file://）全流程复测
- [ ] 实机全流程演练：采购到货 → 手机建档 → 蓝牙打标签 → 贴标 → 扫码 → 点色码 → 收款 → 库存与流水正确（PRD 10.0B）
- [ ] 逐条勾选 PRD 第 10 节全部验收项，未通过的记入缺陷清单并修复

---

## 6. 测试策略

| 层次 | 方式 | 覆盖 |
| --- | --- | --- |
| 纯逻辑 | `node --test tests/`（Node 22 自带，零安装） | 编码生成、库存联动、利润口径、备份恢复、CODE128 |
| 界面 | 浏览器手工 + PRD 验收清单 | 各页面流程、双端布局 |
| 硬件 | 实机（真机 + 打印机） | 扫码识别率、蓝牙打印、连续打印、7 天保存性 |

运行方式：
```bash
cd erp && node --test tests/
```

---

## 7. 运行与发布方式

| 场景 | 方式 |
| --- | --- |
| 电脑本地 | 双击 `index.html`（file://）——**实时扫码不可用，自动降级为拍照识别 + 手输**，其余功能正常 |
| 手机（推荐） | 将 `erp/` 整个目录托管到免费 https 静态空间 → 手机浏览器打开 → "添加到主屏幕" → 实时扫码 + 蓝牙打印全可用 |
| 离线 | 托管版经 Service Worker 缓存后，断网仍可打开使用 |
| 备份纪律 | 首页当日未备份显示提醒条；导出 JSON 存电脑 + 网盘各一份 |

> 需要一台电脑做一次"发布"动作（上传静态文件），日常使用不需要电脑常开。

---

## 8. 风险与对策

| 风险 | 概率 | 对策 |
| --- | --- | --- |
| 蓝牙机型指令不兼容（TSPL/ESC-POS 都不行） | 中 | 先用测试页验证；不兼容则走"导出 PNG/PDF → 厂商 App 打印"兜底（PRD 9 已列） |
| iPhone 无法蓝牙/实时扫码 | 中 | 拍照识别 + 手输 + 电脑端代打；界面自动隐藏不可用入口 |
| 条码打印出来扫不出 | 中 | 静区 ≥2.5mm、模块宽 ≥0.375mm、打印浓度在驱动调、先打测试页（PRD 11.4.3） |
| 数据量大了变卡 | 低 | IndexedDB + 分页（≤300/页）+ 按需索引 |
| 手机清缓存导致数据丢 | 中 | 每日备份提醒 + 每周异地备份 + 备份文件含计数摘要 |
| ES Module 在 file:// 失效 | 高（已规避） | 全程用经典 script 标签，禁止 `type="module"` |

---

## 9. 开工前需要确认的 3 件事

1. **打印机品牌型号** —— 决定默认指令集（TSPL 还是 ESC-POS），以及测试页验证顺序。
2. **手机是 Android 还是 iPhone** —— Android 可实时扫码 + 蓝牙打印；iPhone 走拍照识别 + 分享打印兜底。
3. **类别前缀映射** —— 默认 鞋X / 服装F / 裤K / 配饰P / 包袋B / 其他O，若有自己的习惯（如用数字）请给出。

其余全部按 PRD 执行；需求若变，先改 PRD.md 再改代码。
